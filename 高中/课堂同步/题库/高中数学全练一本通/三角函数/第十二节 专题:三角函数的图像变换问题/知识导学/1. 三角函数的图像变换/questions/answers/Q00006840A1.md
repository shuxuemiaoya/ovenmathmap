---
answer_for: "Q00006840"
answer_provenance: authoritative
answer_source_body_sha256: 24bdb0ef8651fae46cc25a73d09e731817f973867896ce01040f12015c3bcab1
---

> [!faq]- 全练一本通解析
> **【答案】** 详见解析
>
> **【解析】**
> （1）把 $y = sin\left(x + \frac{\pi}{4}\right)$ 的图象向右平移 $\frac{\pi}{4}$ 个单位长度得到 $y = sin\left(x - \frac{\pi}{4}\right) + \frac{\pi}{4} ,$ 即 $y = sin x$ 的图像
> （2）方法一：先把 $y = sin\left( 2x + \frac{\pi}{4}\right)$ 的图象向右平移 $\frac{\pi}{8} 个单位长度得到 \\ y = sin\left( 2\left( x - \frac{\pi}{8}\right) + \frac{\pi}{4}\right), \\ q = 0, \\ q > 0, \\ q < 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0, \\ q < 0, \\ q > 0,\\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 0, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q > 1, \\ q>1,\end{array}$
> 方法二: 先把 $y = \sin\left(2x + \frac{\pi}{4}\right)$ 的图象纵坐标不变, 横坐标扩大为原来的 2 倍, 得到 $y = \sin\left(2 \cdot \frac{1}{2}x + \frac{\pi}{4}\right)$ , 即 $y = \sin\left(x + \frac{\pi}{4}\right)$ , 再向右平移 $\frac{\pi}{4}$ 个单位长度即可得到 $y = \sin x$ .
> 方法三: 逆向思维, 可看成 $y = \sin x$ 经过怎样的变换成 $y = \sin\left(2x + \frac{\pi}{4}\right)$ , 并将变换步骤反向变换即可. 先把 $y = \sin x$ 向左平移 $\frac{\pi}{4}$ 个单位长度得到 $y = \sin\left(x + \frac{\pi}{4}\right)$ , 再将图像纵坐标不变, 横坐标缩小为原来的 $\frac{1}{2}$ , 即可得到 $y = \sin\left(2x + \frac{\pi}{4}\right)$ 的图像.
> 反向变换为: 先将 $y = \sin\left(2x + \frac{\pi}{4}\right)$ 的图像纵坐标不变, 横坐标扩大为原来的 2 倍, 再向右平移 $\frac{\pi}{4}$ 个单位长度.
> （3）方法一: 先把 $y = \sin\left(\frac{1}{2}x - \frac{\pi}{6}\right)$ 的图象向左平移 $\frac{\pi}{3}$ 个单位长度得到 $y = \sin\left(\frac{1}{2}\left(x + \frac{\pi}{3}\right) - \frac{\pi}{6}\right)$ , 即 $y = \sin\left(\frac{1}{2}x\right)$ 再纵坐标不变, 横坐标缩小为原来的 $\frac{1}{2}$ , 即可得到 $y = \sin x$ 的图象.
> 方法二: 先把 $y = \sin\left(\frac{1}{2}x - \frac{\pi}{6}\right)$ 的图象纵坐标不变, 横坐标缩小为原来的 $\frac{1}{2}$ , 得到 $y = \sin\left(\frac{1}{2} \cdot 2x - \frac{\pi}{6}\right)$ , 即 $y = \sin\left(x - \frac{\pi}{6}\right)$ , 再向左平移 $\frac{\pi}{6}$ 个单位长度即可得到 $y = \sin x$ .
> 方法三: 逆向思维, 可看成 $y = \sin x$ 经过怎样的变换成 $y = \sin\left(\frac{1}{2}x - \frac{\pi}{6}\right)$ , 并将变换步骤反向变换即可. $y = \sin x$ 变换为 $y = \sin\left(\frac{1}{2}x - \frac{\pi}{6}\right)$ 步骤为: 纵坐标不变, 横坐标缩小为原来的 $\frac{1}{2}$ , 即可得到 $y = \sin\frac{1}{2}x$ 的图象, 再将图像向右平移 $\frac{\pi}{3}$ 个单位长度得到 $y = \sin\frac{1}{2}\left(x - \frac{\pi}{3}\right)$ , 即 $y = \sin\left(\frac{1}{2}x - \frac{\pi}{6}\right)$ .
> 反向变换为: 先将 $y = \sin\left(\frac{1}{2}x - \frac{\pi}{6}\right)$ 的图象向左平移 $\frac{\pi}{3}$ 个单位长度, 再纵坐标不变, 横坐标缩小为原来的 $\frac{1}{2}$ , 即可得到 $y = \sin x$ 的图象.
> （4）方法一: 先把 $y = 2\cos\left(3x - \frac{\pi}{4}\right) + 1$ 的图象向左平移 $\frac{\pi}{12}$ 个单位长度得到 $y = 2\cos\left(3\left(x + \frac{\pi}{12}\right) - \frac{\pi}{4}\right) + 1$ , 即 $y = 2\cos3x + 1$ , 再纵坐标不变, 横坐标扩大为原来的 3 倍, 得到 $y = 2\cos\left(3\cdot\frac{1}{3}x\right) + 1$ , 即 $y = 2\cos x + 1$ 的图象. 再纵坐标缩小为原来的 $\frac{1}{2}$ , 得到 $y = \cos x + 1$ , 最后向下平移 1 个单位长度得到 $y = \cos x$ 方法二: 先把 $y = 2\cos\left(3x - \frac{\pi}{4}\right) + 1$ 的图象纵坐标不变, 横坐标扩大为原来的 3 倍, 得到 $y = 2\cos\left(3\cdot\frac{1}{3}x - \frac{\pi}{4}\right) + 1$ , 即 $y = 2\cos\left(x - \frac{\pi}{4}\right) + 1$ , 再向左平移 $\frac{\pi}{4}$ 个单位长度即可得到 $y = 2\cos x + 1$ 的图象. 再纵坐标缩小为原来的 $\frac{1}{2}$ , 得到 $y = \cos x + 1$ , 最后向下平移 1 个单位长度得到 $y = \cos x$ .
> 方法三:逆向思维, 可看成 $y = \cos x$ 经过怎样的变换成 $y = 2\cos\left(3x - \frac{\pi}{4}\right) + 1$ , 并将变换步骤反向变换即可. $y = \cos x$ 变换为 $y = 2\cos\left(3x - \frac{\pi}{4}\right) + 1$ 的步骤为: 先把 $y = \cos x$ 的图象纵坐标不变, 横坐标缩小为原来的 $\frac{1}{3}$ , 得到 $y = \cos3x$ , 再向右平移 $\frac{\pi}{12}$ 个单位长度即可得到 $y = \cos3\left(x - \frac{\pi}{12}\right)$ , 即 $y = \cos\left(3x - \frac{\pi}{4}\right)$ 的图象, 再将图像纵坐标扩大为原来的 2 倍, 得到 $y = 2\cos\left(3x - \frac{\pi}{4}\right)$ , 再向上平移 1个单位长度得到 $y = 2\cos\left(3x - \frac{\pi}{4}\right) + 1$ 的图象;
> 反向变换为: 先将 $y = 2\cos\left(3x - \frac{\pi}{4}\right) + 1$ 的图象向左平移 $\frac{\pi}{12}$ 个单位长度, 再纵坐标不变, 横坐标扩大为原来的 3 倍, 再纵坐标缩小为原来的 $\frac{1}{2}$ , 最后向下平移 1 个单位长度得到 $y = \cos x$
