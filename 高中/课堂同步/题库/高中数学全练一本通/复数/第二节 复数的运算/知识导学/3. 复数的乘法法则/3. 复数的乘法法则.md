## 3. 复数的乘法法则

两个复数的乘法可以按照多项式的乘法运算来进行, 只是把 $i^{2}$ 换成 -1, 并把最后结果写成 $a + bi (a, b \in R)$ 的形式.

设 $z_{1}=a+bi$ , $z_{2}=c+di$ ( $a,b,c,d\in R$ ),

则 $z_{1} \cdot z_{2} = (a + bi)(c + di) = ac + adi + bci + bdi^{2} = (ac - bd) + (bc + ad)i$ .

显然两个复数的积仍是复数.
