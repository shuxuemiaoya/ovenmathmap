## 8. 解决对数型复合函数单调性问题的思路

（1） $y=\log_{a}f(x)$ 型：函数 $y=\log_{a}f(x)$ 的单调性与函数 $\mu=f(x)(f(x)>0)$ 的单调性在a>1时相同，在0<a<1时相反；

(2) $y = f(\log_a x)$ 型:一般用换元法,即令 $t = \log_a x$ ,则只需要研究 $t = \log_a x$ 及 $y = f(t)$ 的单调性即可.

注意:研究对数型复合函数的单调性,一般要注意先研究函数的定义域.
