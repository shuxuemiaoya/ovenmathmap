## 5. 底数对对数函数的影响

（1）当底数 a 大小不确定时，必须分 a > 1 和 0 < a < 1 两种情况讨论对数函数的图像与性质.

（2）在第一象限内, 当 a > 1 时, a 的值越大, 对数函数的图像越靠近 x 轴;

当 $0 < a < 1$ 时, $a$ 的值越小, 对数函数的图像越靠近 $y$ 轴.

总结为: 只观察第一象限内的图像, 图像顺时针旋转, 底数由小变大.
