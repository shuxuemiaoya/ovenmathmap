## 9. 解简单对数不等式的方法

(1) 形如 $\log_a x > \log_a b$ 的不等式: 借助 $y = \log_a x$ 的单调性求解, 如果 $a$ 的取值不确定, 需分 $a > 1$ 或 $0 < a < 1$ 两种情况讨论;

（2）形如 $\log_{a}x > b$ 的不等式: 应将 b 化为以 a 为底的对数式的形式, 再借助 $y = \log_{a}x$ 的单调性求解;

（3）形如 $\log_{a}x > \log_{b}x$ 的不等式: 可利用图象求解.
